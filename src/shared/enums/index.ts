export const enum THEME_MODE {
  DARK = 'dark',
  LIGHT = 'light',
}
export const enum ANCHOR {
  HOME = '#home',
  ABOUT = '#about',
  EXPERIENCE = '#experience',
  PROJECTS = '#projects',
  CONTACTS = '#contact',
}
